export default class Item {
    constructor(id, produitId, qte, formatId) {
        this.id = id;
        this.produitId = produitId;
        this.qte = qte;
        this.formatId = formatId;
    }
}
